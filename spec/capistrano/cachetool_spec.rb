require "spec_helper"

RSpec.describe Capistrano::Cachetool do
  it "has a version number" do
    expect(Capistrano::Cachetool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
